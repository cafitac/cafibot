name: Publish npm + PyPI

on:
  push:
    branches:
      - main
    paths:
      - 'hermit-ui/**'
      - 'hermit_agent/**'
      - 'pyproject.toml'
  workflow_dispatch:
    inputs:
      force_publish:
        description: "Publish even if this commit only changes release metadata files"
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  id-token: write

jobs:
  classify_release:
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.classify.outputs.should_publish }}
      reason: ${{ steps.classify.outputs.reason }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Classify release trigger
        id: classify
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          FORCE_PUBLISH: ${{ github.event.inputs.force_publish || 'false' }}
          COMMIT_MESSAGE: ${{ github.event.head_commit.message || '' }}
          BEFORE_SHA: ${{ github.event.before }}
          CURRENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            if [[ "$FORCE_PUBLISH" == "true" ]]; then
              echo "should_publish=true" >> "$GITHUB_OUTPUT"
              echo "reason=manual workflow_dispatch with force_publish=true" >> "$GITHUB_OUTPUT"
            else
              echo "should_publish=false" >> "$GITHUB_OUTPUT"
              echo "reason=manual workflow_dispatch requires force_publish=true" >> "$GITHUB_OUTPUT"
            fi
            exit 0
          fi

          if [[ "$COMMIT_MESSAGE" == *"[skip release]"* ]]; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "reason=commit message requested [skip release]" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [[ "$COMMIT_MESSAGE" =~ ^chore:\ release\ v[0-9] ]]; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "reason=release write-back commit detected" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          mapfile -t changed_files < <(git diff --name-only "$BEFORE_SHA" "$CURRENT_SHA")
          printf 'Changed files (%s):\n' "${#changed_files[@]}"
          printf ' - %s\n' "${changed_files[@]}"

          if [[ ${#changed_files[@]} -eq 0 ]]; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "reason=no changed files detected" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          release_only=true
          for path in "${changed_files[@]}"; do
            case "$path" in
              pyproject.toml|hermit-ui/package.json)
                ;;
              *)
                release_only=false
                break
                ;;
            esac
          done

          if [[ "$release_only" == "true" ]]; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "reason=release metadata only change" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "should_publish=true" >> "$GITHUB_OUTPUT"
          echo "reason=code or package payload changed" >> "$GITHUB_OUTPUT"

  test:
    needs: classify_release
    if: needs.classify_release.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package + test dependencies
        run: pip install -e ".[test]"
      - name: Run pytest suite
        run: pytest tests/ -q

  publish:
    needs:
      - classify_release
      - test
    if: needs.classify_release.outputs.should_publish == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
          cache: npm
          cache-dependency-path: hermit-ui/package-lock.json

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        working-directory: hermit-ui
        run: npm ci

      - name: Build
        working-directory: hermit-ui
        run: npm run build

      - name: Bump to next patch version from registry
        run: |
          PUBLISHED=$(npm view @cafitac/hermit-agent version 2>/dev/null || echo "0.0.0")
          LOCAL=$(node -p "require('./hermit-ui/package.json').version")
          # Use whichever is higher as the base so pre-bumped local versions don't conflict
          BASE=$(printf '%s\n%s\n' "$PUBLISHED" "$LOCAL" | sort -V | tail -1)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$BASE"
          NEXT="${MAJOR}.${MINOR}.$((PATCH + 1))"
          echo "Publishing version $NEXT (registry: $PUBLISHED, local: $LOCAL)"
          echo "NEXT=$NEXT" >> "$GITHUB_ENV"
          # Bump npm package.json (hermit-ui)
          npm version "$NEXT" --no-git-tag-version --prefix hermit-ui
          # Sync pyproject.toml
          sed -i "s/^version = \".*\"/version = \"$NEXT\"/" pyproject.toml

      - name: Verify npm package shape
        working-directory: hermit-ui
        run: npm_config_cache="$(mktemp -d)" npm pack --dry-run

      - name: Publish npm package
        working-directory: hermit-ui
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --provenance

      - name: Build Python distributions
        run: |
          pip install --upgrade pip build
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
          skip-existing: true

      - name: Write back version and tag
        env:
          # GH_PAT must be a fine-grained PAT with Contents: read+write.
          # It is used to bypass the branch protection rule that blocks GITHUB_TOKEN.
          PUSH_TOKEN: ${{ secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${PUSH_TOKEN}@github.com/${{ github.repository }}"
          git add pyproject.toml hermit-ui/package.json
          git diff --staged --quiet || git commit -m "chore: release v${NEXT} [skip ci] [skip release]"
          git tag "v${NEXT}" || echo "Tag v${NEXT} already exists, skipping"
          git push origin HEAD:main --tags || {
            echo "::warning::Write-back push blocked by branch protection."
            echo "::warning::To fix: add 'github-actions[bot]' as bypass actor in repo ruleset settings."
            echo "::warning::npm + PyPI publish already succeeded — skipping write-back."
          }
