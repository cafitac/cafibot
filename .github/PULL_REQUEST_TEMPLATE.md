## What changed

Short description. The diff shows *what*; this should say *why*.

## How it was tested

- [ ] `pytest` passes locally
- [ ] Added tests for the behavior changed (or explained why not)
- [ ] `ruff check` + `ruff format` clean
- [ ] `mypy hermit_agent/` clean (or noted specific `# type: ignore` with reasons)

Paste relevant test output if useful.

## Scope / risk

- Affected areas:
- Breaks compat? (API, config, workflow)
- Any out-of-scope file touched? Why?

## Design notes referenced

(Optional. Link to `.dev/*.md` or `.claude/rules/*.md` if applicable.)

## Checklist

- [ ] Commit messages follow repo style (no `Co-Authored-By` unless human co-author)
- [ ] Read `CONTRIBUTING.md`
- [ ] This PR is focused (single concern)
