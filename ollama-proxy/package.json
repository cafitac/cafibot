{
  "name": "ollama-proxy",
  "version": "1.0.0",
  "description": "Ollama OpenAI-compatible API proxy with auth and concurrency limit",
  "module": "server.ts",
  "scripts": {
    "start": "bun run server.ts"
  },
  "dependencies": {
    "@types/bun": "latest"
  }
}
